def colour(r, g, b):
        if  (1 <= r <= 238 and 54 <= g <= 255 and 1 <= b <= 238):
            return "green"
        
def binary(listname, x):
      first = 0
      last = len(listname) - 1
      while 